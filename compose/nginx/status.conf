# stub_status endpoint for the nginx-prometheus-exporter.
# Listens on 8082, which is NOT mapped to the host — reachable only from the
# compose network (the exporter scrapes it).
server {
    listen 8082;
    server_name _;

    location /stub_status {
        stub_status;
        access_log off;
    }
}
